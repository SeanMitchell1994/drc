k = [0:15];                 % length of time series
y_i = zeros(1,length(k));   % Pre-allocation
y_i(1) = 0;                 % initial condition
mu = 0;

% Solving the iterated solution
for i = 2:length(y_i)
    %y_i(i) = (3.900142000000020 * y_i(i-1)) * (1 - y_i(i-1));
    if ((y_i(i - 1) < 0.5)
        y_i(i) = mu * y_i(i - 1)
    elseif 
        
    end
end